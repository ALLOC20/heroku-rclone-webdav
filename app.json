{
    "name": "rclone",
    "description": "Deploy rclone on Heroku.",
    "keywords": ["rclone"],
    "env": {
        "rclone": {
            "description": "$HOME/.rclone.conf",
            "value": "",
            "required": true
        },
        "parameter": {
            "description": "The following is just an example,Please modify it yourself",
            "value": "OneDrive-alloc: --vfs-cache-mode writes --user admin --pass 7273866 --addr :443",
            "required": true
        }
    },
    "stack": "container"
}
